<style scoped>
.referral-tree-page { padding: 24px; background: linear-gradient(135deg, #0a0e27, #1a1f3a); border-radius: 16px; min-height: 100vh; }

/* Header */
.page-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 32px; padding: 24px; background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.1); border-radius: 16px; }
.page-title { font-size: 32px; font-weight: 800; color: #fff; margin: 0 0 8px 0; }
.page-subtitle { font-size: 14px; color: rgba(255,255,255,0.6); margin: 0; }
.header-btns { display: flex; gap: 12px; }
.btn { padding: 12px 24px; border: none; border-radius: 8px; font-size: 14px; font-weight: 600; cursor: pointer; transition: all 0.3s; }
.btn-update { background: linear-gradient(135deg, #667eea, #764ba2); color: #fff; }
.btn-refresh { background: linear-gradient(135deg, #4facfe, #00f2fe); color: #fff; }
.btn:hover:not(:disabled) { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(102,126,234,0.4); }
.btn:disabled { opacity: 0.6; cursor: not-allowed; }

/* Stats */
.stats-summary { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px; margin-bottom: 32px; }
.stat-box { display: flex; align-items: center; gap: 16px; padding: 24px; background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.1); border-radius: 16px; transition: all 0.3s; }
.stat-box:hover { transform: translateY(-4px); box-shadow: 0 12px 32px rgba(0,0,0,0.3); }
.stat-icon { font-size: 48px; }
.stat-label { font-size: 14px; color: rgba(255,255,255,0.6); margin: 0 0 4px 0; }
.stat-value { font-size: 32px; font-weight: 800; color: #fff; margin: 0; }

/* Levels */
.levels-container { display: flex; flex-direction: column; gap: 16px; margin-bottom: 32px; }
.level-card { display: flex; justify-content: space-between; align-items: center; padding: 20px 24px; background: rgba(255,255,255,0.05); border: 2px solid rgba(255,255,255,0.1); border-radius: 16px; cursor: pointer; transition: all 0.3s; }
.level-card:hover { border-color: rgba(255,255,255,0.2); transform: translateY(-2px); box-shadow: 0 8px 24px rgba(0,0,0,0.3); }
.level-card.active { border-color: rgba(255,255,255,0.3); background: rgba(255,255,255,0.08); }
.level-card-left { display: flex; align-items: center; gap: 16px; }
.level-badge { width: 48px; height: 48px; border-radius: 12px; display: flex; align-items: center; justify-content: center; font-size: 24px; font-weight: 800; color: #fff; }
.badge-1 { background: linear-gradient(135deg, #667eea, #764ba2); }
.badge-2 { background: linear-gradient(135deg, #f093fb, #f5576c); }
.badge-3 { background: linear-gradient(135deg, #4facfe, #00f2fe); }
.badge-4 { background: linear-gradient(135deg, #43e97b, #38f9d7); }
.badge-5 { background: linear-gradient(135deg, #fa709a, #fee140); }
.level-title { font-size: 20px; font-weight: 700; color: #fff; margin: 0 0 4px 0; }
.level-subtitle { font-size: 14px; color: rgba(255,255,255,0.6); margin: 0; }
.level-card-right { display: flex; align-items: center; gap: 24px; }
.level-metrics { display: flex; gap: 24px; }
.level-metrics > div { display: flex; flex-direction: column; align-items: center; }
.metric-value { font-size: 24px; font-weight: 800; color: #fff; }
.metric-label { font-size: 12px; color: rgba(255,255,255,0.6); }
.expand-arrow { font-size: 16px; color: rgba(255,255,255,0.6); transition: transform 0.3s; }
.expand-arrow.rotated { transform: rotate(180deg); }

/* Users List */
.users-list { padding: 24px; background: rgba(255,255,255,0.03); border: 2px solid rgba(255,255,255,0.1); border-top: none; border-bottom-left-radius: 16px; border-bottom-right-radius: 16px; animation: slideDown 0.3s ease; }
@keyframes slideDown { from { opacity: 0; transform: translateY(-10px); } to { opacity: 1; transform: translateY(0); } }
.user-row { display: flex; gap: 12px; padding: 16px; background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.1); border-radius: 12px; transition: all 0.3s; margin-bottom: 12px; }
.user-row:last-child { margin-bottom: 0; }
.user-row:hover { transform: translateY(-2px); box-shadow: 0 8px 16px rgba(0,0,0,0.3); border-color: rgba(255,255,255,0.2); }
.user-avatar { width: 48px; height: 48px; border-radius: 50%; overflow: hidden; flex-shrink: 0; }
.user-avatar img { width: 100%; height: 100%; object-fit: cover; }
.avatar-initials { width: 100%; height: 100%; display: flex; align-items: center; justify-content: center; background: linear-gradient(135deg, #667eea, #764ba2); color: #fff; font-size: 20px; font-weight: 700; }
.user-details { flex: 1; }
.user-name { font-size: 16px; font-weight: 600; color: #fff; margin: 0 0 4px 0; }
.user-code { font-size: 12px; color: rgba(255,255,255,0.6); font-family: monospace; margin: 0; }
.user-stats { display: flex; flex-direction: column; gap: 4px; font-size: 12px; color: rgba(255,255,255,0.8); }
.empty-users { text-align: center; padding: 40px; color: rgba(255,255,255,0.6); }

/* Points Table */
.points-table { padding: 24px; background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.1); border-radius: 16px; }
.table-title { font-size: 24px; font-weight: 700; color: #fff; margin: 0 0 20px 0; }
.table-content { border-radius: 12px; overflow: hidden; }
.table-header, .table-row, .table-footer { display: grid; grid-template-columns: 1fr 1fr 1fr 1fr; gap: 16px; padding: 16px 20px; }
.table-header { background: rgba(255,255,255,0.1); font-weight: 700; color: #fff; }
.table-row { background: rgba(255,255,255,0.03); border-bottom: 1px solid rgba(255,255,255,0.05); color: rgba(255,255,255,0.8); }
.table-row:hover { background: rgba(255,255,255,0.08); }
.table-footer { background: rgba(255,255,255,0.1); font-weight: 700; color: #fff; border-top: 2px solid rgba(255,255,255,0.2); }
.mini-badge { width: 32px; height: 32px; border-radius: 8px; display: flex; align-items: center; justify-content: center; font-size: 16px; font-weight: 700; color: #fff; }
.points-col { font-weight: 600; color: #10eb04; }
.points-col.grand { font-size: 20px; font-weight: 800; }

/* Responsive */
@media (max-width: 768px) {
  .page-header { flex-direction: column; gap: 16px; }
  .header-btns { width: 100%; }
  .btn { flex: 1; }
  .level-card-right { flex-direction: column; gap: 12px; }
  .table-header, .table-row, .table-footer { grid-template-columns: 0.8fr 1fr 0.8fr 1fr; font-size: 14px; }
}
</style>
